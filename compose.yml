services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: funds
      POSTGRES_USER: funds
      POSTGRES_PASSWORD: funds
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
